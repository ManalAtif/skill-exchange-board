# Copy this file to .env.local and fill in real values.
# .env.local is git-ignored — never commit real API URLs/keys if they differ per environment,
# and NEVER put secret keys here at all (frontend env vars are bundled into the
# public JS build and visible to anyone — only put non-secret config here).

VITE_API_BASE_URL=http://localhost:5000/api
